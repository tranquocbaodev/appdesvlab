/* Controllers */
(function() {
    'use strict';

    angular
        .module('app.login')
        .controller('loginCtrl', loginCtrl);

    //login
    function loginCtrl($scope, $filter, $location,dataSvc,$timeout,$window) {
        var vm = this;
        
    }
})();
